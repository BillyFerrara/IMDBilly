<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @feature.title %>
</p>

<p>
  <strong>Duration:</strong>
  <%= @feature.duration %>
</p>

<p>
  <strong>Cast:</strong><br>
  <%= @feature.actors.map { |actor| actor.name}.join(", ") %> <br>
</p>
<%# Was rendering the actual actors objects instead of represenataional string above %>

<%= link_to 'Edit', edit_feature_path(@feature) %> |
<%= link_to 'Back', features_path %>
